完整流程由 `tools/pending_review.py` 與 `tools/verification_sheet.py` 驅動：`python3 tools/pending_review.py status`（結束碼 2 表示有待確認事項）、`list`、`decide --id D-XXX --decision 採納更正 --by "○○○"`、`apply --all --by "○○○"`；或走紙本流程時用 `verification_sheet.py export` 產出 HTML 再手動簽名後以 `apply --results governance/核定紀錄/results-*.json` 回填。