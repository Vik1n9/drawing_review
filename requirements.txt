# Fire Review — 執行相依套件
# 一鍵安裝：bash tools/setup.sh
# 或：python3 -m pip install -r requirements.txt
#
# 核心審圖工具（fire_code_calc.py、regulation_index.py）只用 Python 標準庫，
# 以下套件僅特定交付物需要；未安裝時對應工具會給出明確錯誤與安裝指引。

ezdxf>=1.3,<2        # tools/dxf_svg_review.py：DXF 轉 SVG 圖面標註（交付物1）
openpyxl>=3.1,<4     # tools/standard_checklist_html.py：xlsx 標準表檢核
pymupdf>=1.24,<2     # tools/pdf_annotate.py：平面圖 PDF 紅圈標註
